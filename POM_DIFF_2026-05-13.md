# POM diff vs current dev UI — 2026-05-13

Recon driver: Admin TeamQa (`+998913030519`), Client UI dev. Backend after API
namespace migration to `/api/v1/users/*`. Many list endpoints respond 5xx →
"Network Error" / "Не удалось загрузить ..." баннеры. Layout, headings, buttons,
table column headers и tabs читаются корректно.

> **Конвенция**: «POM impact» = что нужно сделать в `pages/client/*.py`. Все
> рекомендованные локаторы — Playwright sync API, приоритет get_by_role.

---

## Сводная таблица — план правок

| Страница | URL | POM-файл сейчас | Что сделать | Приоритет |
|---|---|---|---|---|
| Главная | `/home` | НЕТ | **CREATE** `home_page.py` (5 виджетов: Текущие начисления, Дней отпуска, График работы, Мои документы, Мои задачи) | High |
| Входящие документы | `/inbox` | `inbox_page.py` | UPDATE: heading «Требуют подписи» (без изменений), 8 status tabs, 6 columns (новый «Действие»), search placeholder уточнить | High |
| Документы | `/documents` | `documents_page.py` | UPDATE: heading=«Документы» (h4), tabs=8 (Все/Черновик/В работе/Завершён/Отменён/В архиве/Отправлен в 1С/Ошибка выгрузки) **совпадают**, columns 7 — **совпадают**. Sidebar link называется «Мои задания» (≠ heading) | High |
| Пользователи | `/members` | `members_page.py` + `member_create_dialog.py` | UPDATE: 6 columns (включая «Системная роль», «Должность»), search placeholder=`Поиск по имени или телефону...`. Dialog: 5 textbox (firstName/lastName/middleName/phone/pinfl) + 3 combobox (Системная роль/Должность/**Отдел** — НОВОЕ). Submit=«Добавить» (ранее у нас был `dialog_submit` ключ, проверить что вернёт «Добавить») | High |
| Организация | `/organization` | `organization_page.py` | UPDATE: heading изменён «Организация» → **«Настройки организации»**. Tabs Данные/Филиалы — без изменений | High |
| Интеграция | `/integration` | `organization_page.py::IntegrationPage` | UPDATE: 3 status tabs (Все/Подключено/Не подключено) добавлены к `IntegrationPage`. Modal title формат `<h2>1C<status>Не подключено</status></h2>` — текущий локатор `get_by_role("dialog", name="…")` не сработает, нужен match по h6 «1C» внутри dialog | High |
| Sidebar | n/a | `sidebar.py` | **MAJOR REWRITE**: 4 новых группы (+ «Мой кабинет», + новая структура), 5 новых ссылок (`/payslip`, `/work-schedule`, `/vacation`, `/persons`, `/routes`), переименования | High |
| Расчётный лист | `/payslip` | НЕТ | CREATE заглушка `payslip_page.py` — пока «Выберите расчётный лист» | Med |
| График работы | `/work-schedule` | НЕТ | CREATE стаб (страница пустая «Нет данных») | Med |
| Отпуск | `/vacation` | НЕТ | CREATE стаб («Нет данных») | Med |
| Физические лица | `/persons` | НЕТ | **CREATE** `persons_page.py`: 9 columns, button «Добавить», search «Поиск...» | Med |
| Должности | `/positions` | `positions_page.py` | UPDATE: только 2 columns (название+действия), button «Добавить должность» | Med |
| Штатные позиции | `/org-positions` | `organization_page.py::OrgPositionsPage` | OK (совпадает) — VERIFY tabs «Список»/«Иерархия», columns по плану | Med |
| Маршруты | `/routes` | `routes_page.py` | UPDATE: heading «Шаблоны маршрутов», 6 columns (Название/Шаги/Статус/Version/Последнее изменение/Действия), button «Создать маршрут» | Med |
| Шаблоны | `/templates` | `templates_page.py` | UPDATE: heading «Шаблоны», button «Добавить шаблон». Columns пока не видны (Network Error) — нужен повторный recon когда бэкенд жив | Med |
| Филиалы | `/branches` | `branches_page.py` | BLOCKED: страница пуста, ошибка «Не удалось загрузить филиалы» — нет heading/buttons/таблицы. Recon отложить | Low |
| Отделы | `/departments` | `departments_page.py` | UPDATE: 6 columns (+ Источник, + Родитель), фильтр по Филиалу/Источнику | Med |
| Системные роли | `/roles` | `roles_page.py` | UPDATE: heading **«Роли и права»** (было «Системные роли» в sidebar), 4 columns | Med |
| User menu | n/a | (в sidebar) | UPDATE: только 2 пункта — Настройки + Выйти (без «Профиль» и т.п.) | Low |

---

## A. /home (был `/dashboard`)

### Что было в POM
Файла `dashboard_page.py` нет. В `sidebar.py` константа `ADMIN_NAV` ссылается на
`/dashboard` (legacy URL).

### Что сейчас на dev
- URL: `/home` (sidebar link `/home`)
- Heading: `<h1>Добро пожаловать, 913030519</h1>` + `<p>среда, 13 мая·Администратор</p>`
- 5 виджет-карточек (h6 заголовки):
  1. **Текущие начисления** — paragraph «Нет данных» + `<button>Все расчётные листы →</button>`
  2. **Вам доступно дней отпуска** — paragraph «Нет данных» + `<button>Подробнее →</button>`
  3. **Ваш график работы** — paragraph «Нет данных» + `<button>Подробнее →</button>`
  4. **Мои документы** — 4 sub-блока: «В работе», «В ожидании», «Завершено», «Отказано» (счётчики `—`)
  5. **Мои задачи** — 3 sub-блока: «Ожидает согласования», «Утверждено», «Отказано»

### POM impact
- CREATE `pages/client/home_page.py` с классом `HomePage`
- В `sidebar.py` заменить `/dashboard` → `/home` в `ADMIN_NAV`
- Smoke-тест `tests/smoke/test_client_login.py` — после login expect URL `/home`, а не `/dashboard`

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", level=1)  # имя меняется (greeting)
self._widget_payslip = page.get_by_role("heading", name="Текущие начисления", level=6)
self._widget_vacation = page.get_by_role("heading", name="Вам доступно дней отпуска", level=6)
self._widget_schedule = page.get_by_role("heading", name="Ваш график работы", level=6)
self._widget_my_docs = page.get_by_role("heading", name="Мои документы", level=6)
self._widget_my_tasks = page.get_by_role("heading", name="Мои задачи", level=6)
self._goto_payslip = page.get_by_role("button", name="Все расчётные листы →")
```

---

## B. /inbox

### Что было в POM
- `URL_PATH = "/inbox"`
- heading «Требуют подписи» (i18n) level=4
- search input
- table

### Что сейчас на dev
- URL: `/inbox?page=1&size=25&status=all&search=`
- Heading: `Требуют подписи` (h4) + paragraph «Документы, ожидающие вашего действия»
- Search placeholder: `Поиск по заголовку или номеру...`
- Tablist (8 tabs, дублирует /documents): Все / Черновик / В работе / Завершён / Отменён / В архиве / Отправлен в 1С / Ошибка выгрузки
- Table columns (6): **Toggle all rows checkbox** | Номер документа | Заголовок | **Действие** | Автор | Дата
- Empty: «Нет документов, ожидающих подписи»

### POM impact
- Подтвердить что `client.inbox.search_placeholder` в i18n = `Поиск по заголовку или номеру...`
- Добавить tabs: текущий POM не покрывает status tabs на /inbox — добавить `STATUS_TABS` константу как в `DocumentsPage`
- Добавить columns константу: `("Номер документа", "Заголовок", "Действие", "Автор", "Дата")` + чекбокс
- Метод `column_header(name)` + метод `tab(name)`

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", name="Требуют подписи", level=4)
self._search = page.get_by_placeholder("Поиск по заголовку или номеру...")
self._tablist = page.get_by_role("tablist").first
self._action_column_header = page.get_by_role("columnheader", name="Действие", exact=True)
```

---

## C. /documents

### Что было в POM
`DocumentsPage`:
- heading «Документы» level=4 ✓
- create button
- 8 STATUS_TABS ✓
- 7 COLUMNS (Номер, Заголовок, Откуда, Куда, Автор, Дата, Статус) ✓

### Что сейчас на dev
- URL: `/documents?page=1&size=25&status=all`
- Heading: `Документы` (h4) + paragraph «Всего 0 документов»
- Button: `Создать документ`
- Tabs (8): Все / Черновик / В работе / Завершён / Отменён / В архиве / Отправлен в 1С / Ошибка выгрузки
- Columns (7): Номер / Заголовок / Откуда / Куда / Автор / Дата / Статус
- Network Error banner under heading (backend break)

### POM impact
- **POM СОВПАДАЕТ** с UI. Дополнительно:
  - Sidebar link к `/documents` называется «**Мои задания**» — это диссонанс. Если будем тестировать что link открывает страницу с heading=«Документы» — тест упадёт на ожидании совпадения. Зафиксировать как known UX bug или в i18n переименовать link.
- create button text: `Создать документ` — проверить совпадение с `t("client.documents.create_button")`

### Рекомендованные локаторы
Без изменений — текущие правильны.

---

## D. /members + Add Employee dialog

### Что было в POM
- `MembersPage`: heading via i18n, add button via i18n, table inside main, status_cell_for_phone использует `.nth(4)`
- `MemberCreateDialog`: 5 textbox (firstName/lastName/middleName/phone/pinfl) + 2 combobox (role/position) + Submit/Cancel

### Что сейчас на dev — page
- URL: `/members?page=1&size=25&search=`
- Heading: `Пользователи` (h4) + paragraph «Управление сотрудниками организации»
- Search placeholder: `Поиск по имени или телефону...`
- Button: `Добавить сотрудника`
- Columns (6): Имя / Телефон / Системная роль / Должность / Статус / Действия
- Empty: «Пользователи не найдены»

### Что сейчас на dev — dialog
- Title: `Добавить сотрудника` (h2)
- Required (отметка `*`): Имя / Фамилия / Телефон / Системная роль
- Optional: Отчество, ПИНФЛ, Должность, Отдел
- Inputs (с placeholder примерами):
  - `name="firstName"` placeholder=`Javlon`
  - `name="lastName"` placeholder=`Abdullayev`
  - `name="middleName"` placeholder=`Otabekovich`
  - `name="phone"` placeholder=`+998911111111`
  - `name="pinfl"` placeholder=`14 цифр`
- Comboboxes: Системная роль *, Должность, **Отдел** ← НОВОЕ
- Buttons: `Отмена`, `Добавить`

### POM impact
- `MembersPage`:
  - Подтвердить i18n: `client.members.title` → `Пользователи`, `add_button` → `Добавить сотрудника`, `search_placeholder` → `Поиск по имени или телефону...`
  - Status column index — теперь nth(4) (проверить с учётом нового column order: Имя=0, Телефон=1, Системная роль=2, Должность=3, Статус=4, Действия=5) — **корректно**
- `MemberCreateDialog`:
  - **ADD** combobox `_department` (label «Отдел»)
  - Submit i18n: `client.members.dialog_submit` должен возвращать `Добавить` (не «Сохранить»)
  - Метод `select_department(label)` симметричный с `select_position`
  - Required-валидацию протестировать: 4 поля required (Имя/Фамилия/Телефон/Системная роль), не 3

### Рекомендованные локаторы
```python
# Page
self._heading = page.get_by_role("heading", name="Пользователи", level=4)
self._add_button = page.get_by_role("button", name="Добавить сотрудника")
self._search = page.get_by_placeholder("Поиск по имени или телефону...")
# Dialog
self._dialog = page.get_by_role("dialog", name="Добавить сотрудника")
self._first_name = self._dialog.get_by_label("Имя", exact=False)  # «Имя *»
self._department = self._dialog.get_by_role("combobox", name="Отдел")
self._submit = self._dialog.get_by_role("button", name="Добавить", exact=True)
```

---

## L. /organization

### Что было в POM
`OrganizationPage`:
- heading «Организация» (i18n) level=4
- TABS = ("Данные", "Филиалы")

### Что сейчас на dev
- URL: `/organization`
- Heading: **`Настройки организации`** (h4) — изменено
- Tabs (2): Данные (selected) / Филиалы
- Body: «Не удалось загрузить данные организации» (backend break)

### POM impact
- Изменить i18n key `client.organization.title` → `Настройки организации`, или хардкодить новый text
- Tabs совпадают, без изменений

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", name="Настройки организации", level=4)
self._tab_data = page.get_by_role("tab", name="Данные", exact=True)
self._tab_branches = page.get_by_role("tab", name="Филиалы", exact=True)
```

---

## M. /integration

### Что было в POM
`IntegrationPage`:
- heading «Интеграция» level=4
- PROVIDERS = (1C, Bitrix24, Налоговая система)
- метод `configure_button_for(name)` — берёт первый «Настроить»

### Что сейчас на dev
- URL: `/integration`
- Heading: `Интеграция` (h4) + paragraph «Подключите внешние системы для обмена данными»
- **Status tabs (3)**: Все (selected) / Подключено / Не подключено ← НОВОЕ
- Cards (h6): 1C / Bitrix24 / Налоговая система — Bitrix24 и Налоговая помечены «Скоро»
- Кнопка «Настроить» только на 1C card
- Click «Настроить» открывает dialog с заголовком (h2 структура): `1C` + status `Не подключено`
- Dialog body пуст (backend down). Все остальные элементы — show key, copy, save, etc. — недоступны для recon

### POM impact
- ADD status tabs constant: `STATUS_TABS = ("Все", "Подключено", "Не подключено")` + метод `tab(name)`
- Modal title: `get_by_role("dialog", name="1C")` НЕ сработает потому что accessible name = «1C\nНе подключено». Использовать:
  ```python
  page.get_by_role("dialog").filter(has=page.get_by_role("heading", name="1C", level=6))
  ```
- Когда backend починят — повторить recon чтобы зафиксировать кнопки «Показать» / «Скопировать» / «Сохранить» внутри modal (BUG-024/028/030 связки)

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", name="Интеграция", level=4)
self._tab_all = page.get_by_role("tab", name="Все", exact=True)
self._tab_connected = page.get_by_role("tab", name="Подключено", exact=True)
self._tab_disconnected = page.get_by_role("tab", name="Не подключено", exact=True)
self._configure_1c = page.get_by_role("button", name="Настроить", exact=True)
# modal:
self._modal_1c = page.get_by_role("dialog").filter(
    has=page.get_by_role("heading", name="1C", level=6)
)
```

---

## Q. Sidebar

### Что было в POM
`ClientSidebar` + константа `ADMIN_NAV`:
- (None, Главная, /dashboard)  ← URL legacy
- 3 группы: Документооборот, Оргструктура, Настройки
- Документооборот: Требуют подписи /inbox, Мои документы /documents, Шаблоны /templates
- Оргструктура: Пользователи, Филиалы, Отделы, Должности, Штатные позиции
- Настройки: Организация, Системные роли, Интеграция

### Что сейчас на dev
Sidebar — 6 групп (некоторые top-level, некоторые collapsible), 17 ссылок.

| Группа | Ссылка | URL | Тип |
|---|---|---|---|
| (top-level) | Главная | `/home` | link |
| Мой кабинет | Расчётный лист | `/payslip` | link (в группе) |
| Мой кабинет | График работы | `/work-schedule` | link |
| Мой кабинет | Отпуск | `/vacation` | link |
| (top-level orphan?) | Мои задания | `/documents` | link |
| (top-level orphan?) | Входящие документы | `/inbox` | link |
| Документооборот | Шаблоны | `/templates` | link (в группе) |
| Документооборот | Маршруты | `/routes` | link (в группе) |
| Оргструктура | Пользователи | `/members` | link |
| Оргструктура | Филиалы | `/branches` | link |
| Оргструктура | Отделы | `/departments` | link |
| Оргструктура | Физические лица | `/persons` | link ← НОВОЕ |
| Оргструктура | Должности | `/positions` | link |
| Оргструктура | Штатные позиции | `/org-positions` | link |
| Настройки | Организация | `/organization` | link |
| Настройки | Системные роли | `/roles` | link (heading=«Роли и права») |
| Настройки | Интеграция | `/integration` | link |

> **Структурные наблюдения**:
> - «Мои задания» и «Входящие документы» расположены **вне** группы «Документооборот» — это два отдельных top-level link-listitem (между «Мой кабинет» и «Документооборот» в DOM)
> - «Шаблоны» и «Маршруты» — внутри группы «Документооборот» (раскрывается)
> - «Маршруты» — НОВЫЙ ENTRY (раньше пользователь шёл через `/templates`?)
> - В sidebar есть toggle темы + переключатель языка RU/O'zbekcha + bottom user button с paragraph=phone и role badge
> - Кнопка «Свернуть» (compact mode) и «Скрыть» (hide sidebar)

### POM impact
**MAJOR REWRITE** `sidebar.py`:
- Полностью переписать `ADMIN_NAV` под новые URL и группы
- `GROUP_NAMES` → 4 шт.: «Мой кабинет», «Документооборот», «Оргструктура», «Настройки»
- Top-level entries (вне группы): «Главная», «Мои задания», «Входящие документы»
- `expand_group()` — текущий метод OK (ищет `[role="button"][aria-label=...]`), но **некликабелен через MCP при коротком viewport**: «Настройки» уходит за viewport. В тестах решается scroll-into-view (Playwright это делает автоматически в новом синтаксисе) или явным wait. Можно добавить `expand_group_via_evaluate(name)` как fallback.
- Учесть compact/hide modes (`Свернуть` / `Скрыть`) — могут изменить ARIA-структуру

### Рекомендованные локаторы
```python
ADMIN_NAV: tuple[tuple[str | None, str, str], ...] = (
    (None, "Главная", "/home"),
    (None, "Мои задания", "/documents"),
    (None, "Входящие документы", "/inbox"),
    ("Мой кабинет", "Расчётный лист", "/payslip"),
    ("Мой кабинет", "График работы", "/work-schedule"),
    ("Мой кабинет", "Отпуск", "/vacation"),
    ("Документооборот", "Шаблоны", "/templates"),
    ("Документооборот", "Маршруты", "/routes"),
    ("Оргструктура", "Пользователи", "/members"),
    ("Оргструктура", "Филиалы", "/branches"),
    ("Оргструктура", "Отделы", "/departments"),
    ("Оргструктура", "Физические лица", "/persons"),
    ("Оргструктура", "Должности", "/positions"),
    ("Оргструктура", "Штатные позиции", "/org-positions"),
    ("Настройки", "Организация", "/organization"),
    ("Настройки", "Системные роли", "/roles"),
    ("Настройки", "Интеграция", "/integration"),
)
```

---

## E. /positions

### Что было в POM
`PositionsPage`: предполагает heading + table, не зафиксировано columns

### Что сейчас на dev
- URL: `/positions?page=1&size=25&search=`
- Heading: `Должности` (h4) + paragraph «Всего 0 должностей»
- Button: `Добавить должность`
- Columns (2): Название должности / Действия
- Empty: «Должности не найдены»

### POM impact
- Зафиксировать `COLUMNS = ("Название должности", "Действия")` (минималистично)
- Add button: `Добавить должность`

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", name="Должности", level=4)
self._add_button = page.get_by_role("button", name="Добавить должность")
```

---

## F. /org-positions

### Что было в POM
`OrgPositionsPage`:
- heading «Штатные позиции»
- add button `+ Добавить позицию`
- search placeholder «Поиск по названию…»
- COLUMNS (6): Название/Отдел/Должность/Сотрудники/При вакантности/Действия
- VIEW_TABS (2): Список/Иерархия

### Что сейчас на dev
**Совпадает 1:1**:
- Heading: `Штатные позиции` (h4) + paragraph «Конкретные позиции в отделах с привязанными сотрудниками.»
- Tabs: Список (selected) / Иерархия
- Button: `+ Добавить позицию`
- Columns: Название / Отдел / Должность / Сотрудники / При вакантности / Действия
- Banner: «Не удалось загрузить позиции» (backend break)

### POM impact
NONE. POM актуален.

---

## G. /persons (НОВАЯ страница)

### Что было в POM
Файла нет.

### Что сейчас на dev
- URL: `/persons`
- Heading: `Физические лица` (h4) + paragraph «0 физических лиц»
- Button: `Добавить`
- Search placeholder: `Поиск...`
- Filter combobox: «Все» (default)
- Columns (9): empty (checkbox?) / ФИО / ПИНФЛ / Дата рождения / Email / Телефон / Источник / Статус / Действия
- Empty: «Нет данных»

### POM impact
CREATE `pages/client/persons_page.py` с классом `PersonsPage`:
- URL_PATH, heading, add_button, search, columns
- Без dialog (recon dialog не делал — нужен следующий проход)

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", name="Физические лица", level=4)
self._add_button = page.get_by_role("button", name="Добавить", exact=True)
self._search = page.get_by_placeholder("Поиск...")
```

---

## H. /branches

### Что было в POM
`BranchesPage` (не читал в этой сессии).

### Что сейчас на dev
- URL: `/branches`
- Body: ТОЛЬКО `Не удалось загрузить филиалы` — нет heading, нет button, нет таблицы
- Backend полностью валит страницу

### POM impact
**BLOCKED**. Recon когда backend починится. Сейчас можно использовать как
negative-asserts: ожидать видимость текста ошибки.

### Рекомендованные локаторы
```python
# negative
self._error = page.get_by_text("Не удалось загрузить филиалы", exact=True)
```

---

## I. /departments

### Что было в POM
`DepartmentsPage` (не читал детально).

### Что сейчас на dev
- URL: `/departments`
- Heading: `Отделы` (h4) + paragraph «Всего 0 отделов»
- Button: `Добавить отдел`
- Filter comboboxes: Филиал, Источник
- Columns (6): Название отдела / Филиал / Родитель / Пользователи / Источник / Действия
- Empty: «Отделы не найдены»

### POM impact
- Подтвердить columns константа
- ADD методы: `filter_by_branch(name)`, `filter_by_source(name)` через combobox

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", name="Отделы", level=4)
self._add_button = page.get_by_role("button", name="Добавить отдел")
self._filter_branch = page.get_by_role("combobox", name="Филиал")
```

---

## J. /templates

### Что было в POM
`TemplatesPage` (не читал детально).

### Что сейчас на dev
- URL: `/templates?page=1&size=25&search=`
- Heading: `Шаблоны` (h4) + paragraph «Всего 0 шаблонов»
- Button: `Добавить шаблон`
- Network Error banner — table columns не отрисованы

### POM impact
- Подтвердить heading + button
- Columns recon отложить до починки backend

---

## K. /routes

### Что было в POM
`RoutesPage` (не читал детально).

### Что сейчас на dev
- URL: `/routes?page=1&size=25&search=`
- Heading: **`Шаблоны маршрутов`** (h4) — обрати внимание, не «Маршруты»
- Button: `Создать маршрут`
- Columns (6): Название / Шаги / Статус / **Version** (англ) / Последнее изменение / Действия
- Network Error banner

### POM impact
- Heading: «Шаблоны маршрутов» (sidebar link «Маршруты», страница «Шаблоны маршрутов»)
- Columns константа
- Колонка `Version` — англ! Возможный i18n bug. Зафиксировать в `Bugs.txt` как minor (если не уже)

### Рекомендованные локаторы
```python
self._heading = page.get_by_role("heading", name="Шаблоны маршрутов", level=4)
self._add_button = page.get_by_role("button", name="Создать маршрут")
```

---

## N. /payslip (НОВАЯ — Мой кабинет)

### Что сейчас на dev
- URL: `/payslip`
- Heading: `Расчётные листы` (h6 — заметь level=6, не h4 как остальные «главные»)
- Body: «Выберите расчётный лист» (placeholder)
- Нет buttons / tabs / table

### POM impact
CREATE `pages/client/payslip_page.py` с минимальным smoke-классом. Реализовать
после того, как появится UI с реальными данными.

---

## O. /work-schedule (НОВАЯ — Мой кабинет)

### Что сейчас на dev
- URL: `/work-schedule`
- Heading: НЕТ
- Body: только «Нет данных»

### POM impact
Stub-класс. Расширить когда появится разметка.

---

## P. /vacation (НОВАЯ — Мой кабинет)

### Что сейчас на dev
- URL: `/vacation`
- Heading: НЕТ
- Body: «Нет данных»

### POM impact
Stub.

---

## R. User menu (bottom sidebar)

### Что сейчас на dev
- Trigger: `<button aria-label="User menu">` с paragraph=phone (`+998913030519`) и role badge `Администратор`
- Menu items (после клика, ищется в `[role="menuitem"]`): **Настройки** / **Выйти**
- Только 2 пункта (нет «Профиль», «Помощь», «Сменить организацию» и т.п.)

### POM impact
- Добавить методы в `ClientSidebar`:
  - `open_user_menu()` → click `button[aria-label="User menu"]`
  - `click_settings()` / `click_logout()`
- Текущий sidebar.py не имеет этих методов — нужно добавить

### Рекомендованные локаторы
```python
self._user_menu_trigger = page.get_by_role("button", name="User menu")
# после открытия:
page.get_by_role("menuitem", name="Настройки", exact=True)
page.get_by_role("menuitem", name="Выйти", exact=True)
```

---

## Не собрано / отложено (требует следующего прохода)

- Add Department dialog — recon не делал
- Add Position dialog — recon не делал
- Add Person dialog — recon не делал
- Document detail page (`/documents/<id>`) — нет тестового документа
- Route Builder UI — backend down
- Modal «1C настройка» содержимое — backend down
- Категории `/categories` — не открывал (если ещё существует)

## Backend break инвентарь (на 2026-05-13 07:21 UTC)

| Page | Symptom |
|---|---|
| /home | API errors в console, виджеты «Нет данных» / «—» |
| /documents | banner «Network Error» |
| /templates | banner «Network Error» |
| /routes | banner «Network Error» |
| /roles | banner «Network Error» |
| /branches | full-page error «Не удалось загрузить филиалы» |
| /organization | banner «Не удалось загрузить данные организации» |
| /integration | banner «Ошибка загрузки» (под tabs) |
| /org-positions | banner «Не удалось загрузить позиции» |
| /persons | пусто, без ошибки (видимо новая ручка работает) |
| /members | пусто, без ошибки |
| /positions | пусто, без ошибки |
| /departments | пусто, без ошибки |
| /payslip | OK (placeholder) |
| /work-schedule | OK (placeholder) |
| /vacation | OK (placeholder) |
| /inbox | OK (пустая таблица без error) |

**Не вижу 5xx → "/api/v1/users/*"** напрямую, но симптомы соответствуют тому,
что list-эндпоинты для documents/templates/routes/roles/branches/organization
возвращают 5xx.
